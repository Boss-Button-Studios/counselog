"""Entry point for `./counselogd`. The service itself lands in phase 3."""

import sys


def main() -> int:
    print("counselogd — scaffold only. The TLS service arrives in phase 3.")
    print("This machine holds no key material at rest by design.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
